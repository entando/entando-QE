- User Management > Profile Type: Viene visualizzato per qualche istante il messaggio su sfondo rosso "There are NO PROFILE TYPE avaiable" prima del caricamento della tabella;
- User Management > Profile Type > Add: Non funziona più l'aggiunta di un profile type;
- User Management > Profile Type > Add: Quando funzionava, era obbligatorio selezionare un 'Type' dal menù a tendina, ma non era obbligatorio aggiungerlo (non ha senso e comunque non dovrebbe essere un campo richiesto);
- User Management > Groups > Add: Non viene eseguito il controllo sul campo code in tempo reale durante la compilazione del campo name;
- User Management: Nelle varie sezioni, i pulsanti per l'aggiunta di un elemento, non hano una etichetta comune (Add, New);
- User Management > Profile types: Il titolo di questa pagina è "Profile type" senza la "s" finale così come nel menù;
- User Management > Profile types: A differenza di tutte le altre pagine che contengono una tabella di questo tipo, questa non prevede lo "spinner" che indica il "loading". Lo spinner dovrebbe appare ogni volta che si aggiorna la pagina oppure quando si sfoglia la tabella;
- User Management > Users: Nel menù Actions, "Manage Autorization" è senza la "h";
- User Management > Users: Edit profile non funziona (errore 404);
- User Management > Roles > Add: C'è uno switch con un etichetta con un misto di due lingue "Gestione Web Dynamic Forms";
- User Management > Groups: Gli elementi sono oridnati per codice e non per nome;
- User Management > Profile types: Gli elementi non hanno un criterio di ordinamento chiaro;
- User Management > Users: Non esegue la ricerca. Viene eseguita solo se viene cliccato "User with a profile" e poi dopo funziona anche su "All";
- Page Designer > Page Tree: Nel menù "Action", gli elementi scritti in grigio ("unpublish" oppure "delete" a seconda dei casi) sono comunque cliccabili e appare la finestra di richiesta di conferma;
- Page Designer > Details: in molte sezioni, fra cui questa, non c'è modo di tornare alla pagina precedente usando il percorso
